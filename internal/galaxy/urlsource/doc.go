// Package urlsource is the I/O-free grammar of a url collection or role
// source: the tarball URL, its url+<url>#sha256:<hex> locator and the
// GO_GALAXY_URL_* binding prefix. It imports only helpers, as gitsource does.
package urlsource
