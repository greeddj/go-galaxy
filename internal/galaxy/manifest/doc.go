// Package manifest reads and cross-checks the metadata documents a collection
// artifact carries. It writes nothing, so the code whose verdict decides an
// install can never change what is installed.
package manifest
