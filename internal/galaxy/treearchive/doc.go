// Package treearchive is the one production tar writer: it walks a Source,
// never the filesystem, under the helpers archive caps and writes the
// deterministic tar.gz that collectionbuild and rolebuild both produce.
package treearchive
